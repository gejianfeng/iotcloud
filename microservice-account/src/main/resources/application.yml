server:
  port: 20040

spring:
  application:
    name: account-service
  redis:
    database: 1
    host: 10.211.55.5
    port: 6379
    password: QE@ip_ZC@bm_123
    jedis:
      pool:
        max-active: 200
        max-idle: 10
  datasource:
    type: com.alibaba.druid.pool.DruidDataSource
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://10.211.55.5:5432/iotcloud
    username: postgres
    password: QE@ip_ZC@bm_123
    druid:
      initial-size: 5
      min-idle: 5
      max-active: 20
      max-wait: 60000
      time-between-eviction-runs-millis: 60000
      min-evictable-idle-time-millis: 3000000
      filter: stat,wall,log4j
      connection-properties: druid.stat.mergeSql=true;durid.stat.slowSqlMillis=5000
      use-global-data-source-stat: true

eureka:
  instance:
    hostname: iotaccount1.com
  client:
    service-url:
      defaultZone: http://ioteureka1.com:20000/eureka,http://ioteureka2.com:20001/eureka

management:
  endpoints:
    web:
      exposure:
        include: "*"
  endpoint:
    health:
      show-details: always
    shutdown:
      enabled: true